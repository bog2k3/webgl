import { Camera } from "./camera";
import { Matrix } from "./math/matrix";
import { Vector } from "./math/vector";
export declare class Viewport {
    constructor(x: number, y: number, w: number, h: number);
    bkColor(): Vector;
    setBkColor(c: Vector): void;
    camera(): Camera;
    width(): number;
    height(): number;
    aspectRatio(): number;
    position(): Vector;
    isEnabled(): boolean;
    containsPoint(p: Vector): boolean;
    screenRect(): Vector;
    project(point: Vector): Vector;
    unproject(point: Vector): Vector;
    viewport2Uniform(): Matrix;
    setEnabled(enabled: boolean): void;
    setArea(vpX: number, vpY: number, vpW: number, vpH: number): void;
    clear(): void;
    renderList(list: unknown[], ctx: unknown): void;
    prepareForRender(): void;
    resetAfterRender(): void;
    private viewportArea_;
    private camera_;
    private enabled_;
    private backgroundColor_;
    private mViewport2Uniform_;
    private mPV_cache_;
    private mPV_inv_cache_;
    private updateVP2UMat;
}
